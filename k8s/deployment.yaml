apiVersion: apps/v1
kind: Deployment
metadata:
  name: sbm-engine
  namespace: tenant-d
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sbm-engine
  template:
    metadata:
      labels:
        app: sbm-engine
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: engine
          image: ghcr.io/fredd80/sbm-engine:${IMAGE_TAG}
          command: ["python", "cli.py", "scheduler"]
          envFrom:
            - configMapRef:
                name: sbm-config
            - secretRef:
                name: sbm-secrets
          ports:
            - containerPort: 9090
              name: metrics
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
